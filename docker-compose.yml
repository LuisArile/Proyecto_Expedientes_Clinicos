services:
  database:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: sql-clinica
    env_file:
      - .env.docker
    ports:
      - "1433:1433"
    volumes:
      - sql_data_clinica:/var/opt/mssql
    restart: always
    # healthcheck:
    #   test: ["CMD-SHELL", "/opt/mssql-tools/bin/sqlcmd -S localhost -U sa -P ${MSSQL_SA_PASSWORD:-Kaizen2026!} -Q 'SELECT 1' || exit 1"]
    #   interval: 5s
    #   timeout: 5s
    #   retries: 15
    #   start_period: 30s

  # backend:
  #     build: ./backend
  #     container_name: api-clinica
  #     volumes:
  #       - ./backend:/app
  #       - /app/node_modules
  #     env_file:
  #       - .env.docker
  #     ports:
  #       - "3000:3000"
  #     depends_on:
  #       - database
  #     restart: on-failure
  #     command: >
  #       sh -c "echo 'Esperando a la base de datos...' && 
  #            sleep 15 && 
  #            npx prisma generate && 
  #            npx prisma migrate deploy && 
  #            npx prisma db seed && 
  #            npm run dev"

  # frontend:
  #     build: ./frontend
  #     volumes:
  #     - ./frontend:/app
  #     - /app/node_modules
  #     container_name: web-clinica
  #     ports:
  #       - "5173:5173"
  #     env_file:
  #       - ./frontend/.env
  #     depends_on:
  #       - backend

volumes:
  sql_data_clinica: